// Retired in the professional design pass. Kept as a no-op so existing
// imports across the site continue to compile without noise.
export function AnimatedBlobs(_: { className?: string } = {}) {
  return null;
}